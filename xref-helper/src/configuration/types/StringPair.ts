
/**
 * A string pair is a key-value pair where both the key and the value are strings.
 */
export type StringPair = {
    /**
     * The key of the string pair, which is used to identify the value.
     */
    [key: string]: string;
};
